// Espelha backend/src/events/events.constants.ts (EVENTS_LIMITS.MAX_EVENT_CAPACITY)
// — só pra mostrar o aviso no front antes do submit; a validação real é
// sempre a do backend.
export const EVENTS_MAX_CAPACITY = 300;
